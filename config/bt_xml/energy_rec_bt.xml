<root>
    <TreeNodesModel>
        <Condition ID="are_plugins_running">
            <input_port name="all_plugin_running" default="false">whether all the plugins are successfully running or not</input_port>
            <input_port name="plugin_names">names of the plugins of which we need to check the running state before executing the main control tree</input_port>
        </Condition>
        <Action ID="go2takeoff_config">
            <input_port name="jump_ready" default="false">if true, we are ready to move to next phase</input_port>
            <output_port name="trigger_approach_traj" default="false">when true, will send a signal to the plugin for moving the robot to the inital configuration for the demo</output_port>
        </Action>
        <Action ID="perform_takeoff">
            <input_port name="takeoff_detected" default="false">go to next phase if true</input_port>
            <output_port name="trigger_takeoff" default="false">if true, trigger the plugin which performs the takeoff</output_port>
        </Action>
        <Action ID="power_monitor"/>
        <Condition ID="recov_energy">
            <input_port name="current_e_r" default="0.0">amount of recovered energy during the post-impact phase (provided by a sutable component)</input_port>
        </Condition>
        <Action ID="start_plugins">
            <input_port name="plugin_names">vector of plugin names,  which need to be started</input_port>
        </Action>
        <Condition ID="temperature_ok">
            <input_port name="temp_ok" default="false">whether driver temperatures are BOTH ok</input_port>
        </Condition>
        <Action ID="wait_for_actuators_cooling">
            <output_port name="stop_robot" default="false">If true, signal all plugins to stop the robot</output_port>
        </Action>
    </TreeNodesModel>
</root>

